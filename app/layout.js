export const metadata = {
  title: 'OpenClaw Installation Guide',
  description: 'Complete troubleshooting guide for OpenClaw setup',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
